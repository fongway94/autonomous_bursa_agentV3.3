streamlit>=1.30.0
# PINNED (was yfinance>=0.2.30). Streamlit Cloud reinstalls deps on redeploy,
# so `>=` silently pulled whatever yfinance shipped most recently. Between
# 0.2.30 and 1.5.x the HTTP stack was swapped from `requests` to `curl_cffi`
# (Yahoo began TLS-fingerprinting plain requests traffic), and Ticker.history
# became (*args, **kwargs) so bad kwargs now fail at runtime instead of import.
# An unnoticed major bump = silent scan degradation. Bump deliberately, after
# a green CI run.
yfinance==1.5.2
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0
scikit-learn>=1.2.0
scipy>=1.10.0
joblib>=1.2.0
pytest>=7.4.0
requests>=2.28.0
# v3.6: NYSE/NASDAQ trading calendar auto-extends — no annual maintenance.
pandas_market_calendars>=4.4.0
# Optional: real-time Bursa data when Moomoo OpenD is running
# locally on 127.0.0.1:11111. Falls back to yfinance automatically.
# v3.6: same package also serves US.{symbol} for the US market profile.
moomoo-api>=8.0.0
